export function access(req, res) {
  res.send("Acceso de usuario");
}
export function profile(req, res) {
  res.send("Perfil de usuario");
}
export function save(req, res) {
  res.send("Registro de usuario");
}
